import math

n = 0.88
p = 1000

# Corrected the comparison to compare 'n' instead of 'np'
print(n*p >= 10)

# Corrected the formula, assuming you want to calculate standard deviation for binomial distribution
print(math.sqrt((p*(1 - p)) / n))
      